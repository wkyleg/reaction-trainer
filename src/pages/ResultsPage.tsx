export function ResultsPage() {
  return (
    <div className="min-h-screen flex items-center justify-center" style={{ background: 'var(--color-bg)' }}>
      <p style={{ fontFamily: "'Orbitron', sans-serif", color: 'var(--color-primary)' }}>
        RESULTS — COMING SOON
      </p>
    </div>
  );
}
